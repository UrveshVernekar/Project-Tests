It cannot be run on Python 3.8 and above versions.

So please install Python 3.7 or below(I used Python 3.7.7 for this).

Installations:
  pip install kivy
  pip install kivy-deps.angle
  pip install kivy-deps.glew
  pip install kivy-deps.gstreamer
  pip install kivy-deps.sdl2
  pip install kivymd
